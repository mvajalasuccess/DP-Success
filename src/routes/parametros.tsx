import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowLeft, Settings2 } from "lucide-react";
import { Card } from "@/components/ui/card";
export const Route=createFileRoute("/parametros")({component:Parametros});
const rules=[["Hora extra","60%"],["Hora extra noturna","60% + 20%"],["Adicional noturno","20%"],["Domingo / feriado","100%"],["Interjornada","50%"],["Divisor padrão","220 horas"]]; 
function Parametros(){return <div className="min-h-screen bg-background"><header className="border-b px-6 py-4"><div className="mx-auto flex max-w-[1500px] items-center justify-between"><Link to="/" className="flex items-center gap-2 text-sm text-muted-foreground"><ArrowLeft className="h-4 w-4"/>Voltar</Link><span className="font-semibold">DP Success · Parâmetros</span></div></header><main className="mx-auto max-w-[1000px] px-6 py-7"><p className="text-sm font-medium text-primary">Configurações</p><h1 className="mt-1 text-3xl font-bold">Parâmetros de cálculo</h1><p className="mt-1 text-sm text-muted-foreground">Regras configuráveis usadas pelos lançamentos e fechamentos.</p><Card className="mt-6 overflow-hidden"><div className="flex items-center gap-3 border-b p-5"><Settings2 className="h-5 w-5 text-primary"/><div><h2 className="font-bold">Regras atuais</h2><p className="text-xs text-muted-foreground">Valores definidos conforme as regras informadas para o sistema.</p></div></div>{rules.map(([name,value])=><div key={name} className="flex items-center justify-between border-b px-5 py-4 last:border-0"><span className="text-sm font-medium">{name}</span><span className="rounded-md bg-muted px-3 py-1 text-sm font-semibold">{value}</span></div>)}</Card></main></div>}
