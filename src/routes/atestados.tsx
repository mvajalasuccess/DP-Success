import { createFileRoute, Link } from "@tanstack/react-router";import { ArrowLeft,Plus,FileText } from "lucide-react";import { Card } from "@/components/ui/card";import {useEffect,useState} from "react";import {supabase} from "@/integrations/supabase/client";
export const Route=createFileRoute("/atestados")({component:Atestados});
function Atestados(){const [rows,setRows]=useState<any[]>([]),[employees,setEmployees]=useState<any[]>([]),[open,setOpen]=useState(false),[form,setForm]=useState({employee_id:"",start_date:"",end_date:"",days:"",hours:"",cid:"",notes:"",entry_date:""}),[error,setError]=useState("");
async function load(){const {data,error}=await (supabase as any).from("medical_certificates").select("id,employee_id,start_date,end_date,days,hours,cid,notes,employees(full_name)").order("start_date",{ascending:false});if(error)setError(error.message);else setRows(data??[])}useEffect(()=>{void load();void(async()=>{const {data}=await (supabase as any).from("employees").select("id,full_name").eq("active",true).order("full_name");setEmployees(data??[])})()},[]);
async function save(){if(!form.employee_id||!form.start_date||!form.end_date)return;const {error}=await (supabase as any).from("medical_certificates").insert({...form,days:form.days?Number(form.days):null,hours:form.hours?Number(form.hours):null});if(error)setError(error.message);else{setOpen(false);setForm({employee_id:"",start_date:"",end_date:"",days:"",hours:"",cid:"",notes:"",entry_date:""});await load()}}
return <div className="min-h-screen bg-background"><header className="border-b px-6 py-4"><div className="mx-auto flex max-w-[1500px] items-center justify-between"><Link to="/" className="flex gap-2 text-sm text-muted-foreground"><ArrowLeft className="h-4 w-4"/>Voltar</Link><b>DP Success · Atestados</b></div></header><main className="mx-auto max-w-[1500px] px-6 py-7"><div className="flex justify-between items-end"><div><p className="text-sm text-primary">Ponto</p><h1 className="text-3xl font-bold">Atestados</h1><p className="text-sm text-muted-foreground">Controle de períodos, horas, CID e documentos.</p></div><button onClick={()=>setOpen(true)} className="rounded-lg bg-primary px-4 py-2.5 text-sm text-primary-foreground"><Plus className="inline h-4 w-4 mr-2"/>Novo atestado</button></div>{error&&<p className="mt-4 text-sm text-destructive">{error}</p>}<Card className="mt-6 overflow-hidden"><div className="border-b p-4 flex gap-3"><FileText className="h-5 w-5 text-primary"/><p className="text-xs text-muted-foreground">Dados médicos são sensíveis e devem ter acesso restrito.</p></div><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-muted/40 text-xs text-muted-foreground"><tr><th className="px-5 py-3">Funcionário</th><th className="px-5 py-3">Início</th><th className="px-5 py-3">Fim</th><th className="px-5 py-3">Dias</th><th className="px-5 py-3">CID</th></tr></thead><tbody className="divide-y">{rows.map(r=><tr key={r.id}><td className="px-5 py-4 font-medium">{r.employees?.full_name}</td><td className="px-5 py-4">{r.start_date}</td><td className="px-5 py-4">{r.end_date}</td><td className="px-5 py-4">{r.days??"—"}</td><td className="px-5 py-4">{r.cid??"—"}</td></tr>)}</tbody></table></div></Card></main>{open&&<div className="fixed inset-0 flex items-center justify-center bg-black/40 p-4"><Card className="w-full max-w-xl p-6"><h2 className="text-xl font-bold">Novo atestado</h2><div className="mt-4 grid gap-3 md:grid-cols-2"><select value={form.employee_id} onChange={e=>setForm({...form,employee_id:e.target.value})} className="rounded-lg border px-3 py-2 md:col-span-2"><option value="">Funcionário</option>{employees.map(e=><option key={e.id} value={e.id}>{e.full_name}</option>)}</select><input type="date" value={form.start_date} onChange={e=>setForm({...form,start_date:e.target.value})} className="rounded-lg border px-3 py-2"/><input type="date" value={form.end_date} onChange={e=>setForm({...form,end_date:e.target.value})} className="rounded-lg border px-3 py-2"/><input type="date" placeholder="Data de entrada" value={form.entry_date} onChange={e=>setForm({...form,entry_date:e.target.value})} className="rounded-lg border px-3 py-2"/><input type="number" placeholder="Dias" value={form.days} onChange={e=>setForm({...form,days:e.target.value})} className="rounded-lg border px-3 py-2"/><input type="number" placeholder="Horas" value={form.hours} onChange={e=>setForm({...form,hours:e.target.value})} className="rounded-lg border px-3 py-2"/><input placeholder="CID" value={form.cid} onChange={e=>setForm({...form,cid:e.target.value})} className="rounded-lg border px-3 py-2 md:col-span-2"/><textarea placeholder="Observações" value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})} className="rounded-lg border px-3 py-2 md:col-span-2"/></div><div className="mt-4 flex justify-end gap-2"><button onClick={()=>setOpen(false)} className="rounded-lg border px-4 py-2">Cancelar</button><button onClick={()=>void save()} className="rounded-lg bg-primary px-4 py-2 text-primary-foreground">Salvar</button></div></Card></div>}</div>}