import{createFileRoute,Link}from"@tanstack/react-router";import{ArrowLeft}from"lucide-react";import{Card}from"@/components/ui/card";import{useEffect,useState}from"react";import{supabase}from"@/integrations/supabase/client";export const Route=createFileRoute("/comparativos")({component:Comparativos});function Comparativos(){const[rows,setRows]=useState<any[]>([]),[error,setError]=useState("");useEffect(()=>{void(async()=>{const{data,error}=await(supabase as any).from("competencies").select("id,name,start_date,end_date,competence_employee_balances(employee_id,closing_minutes,employees(full_name))").order("start_date",{ascending:false}).limit(2);if(error)setError(error.message);else{const cs=data??[];const a=cs[1]?.competence_employee_balances??[],b=cs[0]?.competence_employee_balances??[];const bm=new Map(b.map((x:any)=>[x.employee_id,x]));setRows(a.map((x:any)=>{const y=bm.get(x.employee_id);return{employee:x.employees?.full_name,previous:x.closing_minutes||0,current:y?.closing_minutes||0,diff:(y?.closing_minutes||0)-(x.closing_minutes||0)}}))}})()},[]);const fmt=(n:number)=>{const s=n<0?"-":"";n=Math.abs(n);return s+Math.floor(n/60)+":"+String(n%60).padStart(2,"0")};return <div className="min-h-screen bg-background"><header className="border-b px-6 py-4"><div className="mx-auto flex max-w-[1500px] justify-between"><Link to="/" className="text-sm text-muted-foreground">← Voltar</Link><b>DP Success · Comparativos</b></div></header><main className="mx-auto max-w-[1200px] px-6 py-7"><p className="text-sm text-primary">Gestão</p><h1 className="text-3xl font-bold">Comparativos</h1>{error&&<p className="mt-4 text-sm text-destructive">{error}</p>}<Card className="mt-6 overflow-hidden"><table className="w-full text-left text-sm"><thead className="bg-muted/40 text-xs text-muted-foreground"><tr><th className="px-5 py-3">Funcionário</th><th className="px-5 py-3">Anterior</th><th className="px-5 py-3">Atual</th><th className="px-5 py-3">Variação</th></tr></thead><tbody className="divide-y">{rows.map(r=><tr key={r.employee}><td className="px-5 py-4 font-medium">{r.employee}</td><td className="px-5 py-4">{fmt(r.previous)}</td><td className="px-5 py-4">{fmt(r.current)}</td><td className="px-5 py-4 font-bold">{fmt(r.diff)}</td></tr>)}</tbody></table></Card></main></div>}