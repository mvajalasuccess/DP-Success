import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowLeft, TrendingUp, Users, Clock3, CalendarX2 } from "lucide-react";
import { Card } from "@/components/ui/card";
export const Route=createFileRoute("/kpis")({component:Kpis});
const metrics=[["Absenteísmo","3,8%","Horas perdidas ÷ horas previstas",CalendarX2],["HE / horas trabalhadas","4,6%","Participação de horas extras",Clock3],["Média de HE / funcionário","12:24","Horas por colaborador",TrendingUp],["Funcionários c/ banco positivo","84%","Saldo acima de zero",Users]];
function Kpis(){return <div className="min-h-screen bg-background"><header className="border-b px-6 py-4"><div className="mx-auto flex max-w-[1500px] items-center justify-between"><Link to="/" className="flex items-center gap-2 text-sm text-muted-foreground"><ArrowLeft className="h-4 w-4"/>Voltar</Link><span className="font-semibold">DP Success · KPIs</span></div></header><main className="mx-auto max-w-[1500px] px-6 py-7"><p className="text-sm font-medium text-primary">Gestão</p><h1 className="mt-1 text-3xl font-bold">KPIs de RH e DP</h1><p className="mt-1 text-sm text-muted-foreground">Indicadores para acompanhar ponto, absenteísmo, banco de horas e horas extras.</p><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{metrics.map(([name,value,desc,Icon])=><Card key={name as string} className="p-5"><Icon className="h-5 w-5 text-primary"/><p className="mt-4 text-sm text-muted-foreground">{name as string}</p><p className="mt-1 text-2xl font-bold">{value as string}</p><p className="mt-1 text-xs text-muted-foreground">{desc as string}</p></Card>)}</div><Card className="mt-6 p-6"><h2 className="font-bold">Fórmula de absenteísmo</h2><p className="mt-2 text-sm text-muted-foreground">Percentual configurável: horas de ausência consideradas ÷ horas previstas no período × 100.</p></Card></main></div>}
